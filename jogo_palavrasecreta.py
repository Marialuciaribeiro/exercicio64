palavras_secreta = "paralelepipedo"
letras_acertadas = ""
numero_tentativas += 1


while True:

        letras_digitada = input("digite uma letra! \n")
        numero_tentativas += 
        
        if len(letra_digitada) > 1:
                print("Digite apenas uma letra.")
                continue
        if letra_digitada in palavra_secreta:
                letras_acertadas += letra+digitada




















